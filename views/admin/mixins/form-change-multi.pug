mixin form-change-multi(path)
  form(
    action=path
    method="POST"
    class="space-y-4 md:space-y-0 md:flex md:gap-4"
    form-change-multi
  ) 
    div(class=" *:w-full")
        select(
            name="type" 
            class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
        )
            option(disabled selected value=null) --- Chọn hành động ---
            option(value="active") Hoạt động
            option(value="inactive") Dừng hoạt động
            option(value="delete-all") Xóa Tất cả 
            option(value="change-position") Thay đổi vị trí
    
    div(class="hidden md:block")
        input(
            type="text"
            name="ids"
            value=""
            class="hidden"
        )
    
    button(
        type="submit" 
        class="w-full md:w-auto px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors duration-200"
    ) Áp dụng
