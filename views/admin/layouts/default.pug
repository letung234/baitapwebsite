doctype html
html
  head
    title #{pageTitle}
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css")
    link(rel="stylesheet", href="/admin/css/style.css")
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css")
    link(rel="stylesheet" href="/css/tailwindcss.css")
  body(class="bg-gray-100")
      if(role.permission.length > 0)
        include ../partials/header.pug

        .body(class="flex flex-col md:flex-row min-h-screen")
          include ../partials/sider.pug

          main(class="flex-1 pt-16 md:pt-20 transition-all duration-300 mx-3") 
            .container(class="mx-auto max-w-full w-full px-0")
                block main
        include ../partials/footer.pug
        script(src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js") 
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js")
        script(type="application/javascript" src= "/tinymce/tinymce.min.js")
        script(type="application/javascript" src= "/admin/js/tinymce-config.js")
        script(src="/admin/js/script.js")
        script(src="/js/validates.js")
        script(src="https://cdn.jsdelivr.net/npm/toastify-js")
      
      else 
        .h-screen.flex.items-center.justify-center.bg-gradient-to-br.from-blue-50.to-indigo-50
          .text-center.max-w-md.mx-4(
            class="animate-[slideIn_0.5s_ease-out]"
          )
            .inline-flex.items-center.justify-center.w-24.h-24.mb-6.rounded-full.bg-red-50
              i.fas.fa-lock.text-4xl.text-red-600
              
            h1.text-3xl.font-extrabold.text-gray-800.mb-3(
              class="tracking-tight"
            ) Truy cập bị từ chối
            
            p.text-gray-600.mb-6.text-lg
              | Tài khoản của bạn không có quyền xem trang này. 
              br
              | Mã quyền: #{role.roleId}

            .flex.flex-col.items-center.gap-3
              a(
                href="/" 
                class="w-full sm:w-auto px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-all duration-200 transform hover:scale-[1.02] shadow-lg hover:shadow-xl"
              )
                i.fas.fa-home.mr-2
                | Về trang chủ
              
              a(
                href="mailto:admin@example.com" 
                class="text-blue-600 hover:text-blue-700 text-sm font-medium hover:underline"
              )
                i.fas.fa-envelope.mr-2
                | Liên hệ hỗ trợ